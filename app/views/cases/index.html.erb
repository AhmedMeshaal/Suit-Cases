<h1>Listing cases</h1>
 



<table class="table table-striped table-hover" id="cases-table">
  <thead class="thead-dark">
  <tr>
    <th>Created By</th>
    <th>Subject</th>
    <th>Detail</th>
    <th>Oppossing Lawyer</th>
    <th></th>
  </tr>
  </thead>
 
  <% @case.each do |cases| %>
    <tr>
      <td><%= cases.user.name%></td>
      <td><%= cases.subject %></td>
      <td><%= cases.detail %></td>
      <td><%= cases.opposing_lawyer %></td>
      

      <td><%= link_to 'Show', case_path(cases) %></td>
      
      <% if current_user.id == cases.user_id %>
      <td><%= link_to 'Edit', edit_case_path(cases) %></td>
      <td><%= link_to 'Profile', cases_profile_path(cases) %></td>
      <td><%= link_to 'Destroy', case_path(cases),
              method: :delete,
              data: { confirm: 'Are you sure?' } %></td>
              <% end%>
      
    </tr>
  <% end %>
</table>